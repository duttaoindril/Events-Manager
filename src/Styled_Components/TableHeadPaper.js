import styled from 'styled-components';
import { Paper } from '@material-ui/core';

const TableHeadPaper = styled(Paper)`
  width: calc(100% - 40px);
  margin: 20px;
`;

export default TableHeadPaper;
