import styled from 'styled-components';
import { Paper } from '@material-ui/core';

const ListWrapper = styled(Paper)`
  width: calc(100% - 40px);
  height: auto; // calc(100% - 173px);
  margin: 20px;
  padding: 15px;
`;

export default ListWrapper;
